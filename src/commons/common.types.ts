export enum Status {
  Active = "active",
  Inactive = "inactive",
  Deleted = "deleted",
}

export enum Role {
  Admin = "admin",
  User = "user",
}

export enum DeliveryStatus {
  Failed = "failed",
  Sent = "sent",
  Received = "received",
  Seen = "seen",
}
